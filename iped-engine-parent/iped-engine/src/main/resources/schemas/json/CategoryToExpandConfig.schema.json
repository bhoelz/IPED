{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/CategoryToExpandConfig.schema.json",
  "title": "Category To Expand Configuration",
  "description": "getConfiguration() returns a Set<String> of category names, not a structured object. Schema intentionally only checks that until this config is migrated to a named POJO.",
  "type": "array",
  "items": {
    "type": "string"
  }
}
