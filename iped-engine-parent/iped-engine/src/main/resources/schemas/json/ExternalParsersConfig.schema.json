{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/ExternalParsersConfig.schema.json",
  "title": "External Parsers Configuration",
  "description": "External tool/parser wiring (ExternalParsers.xml). Currently stored and exposed as a raw XML document, not structured JSON — this schema intentionally only checks that, until ExternalParsers.xml is migrated to a structured format.",
  "type": "string"
}
