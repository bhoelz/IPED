{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/AIFiltersConfig.schema.json",
  "title": "AI Filters Configuration",
  "description": "getConfiguration() returns the raw config file text, not a structured object. Schema intentionally only checks that until this config is migrated to a named POJO.",
  "type": "string"
}
