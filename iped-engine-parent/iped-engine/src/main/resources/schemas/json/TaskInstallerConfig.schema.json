{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/TaskInstallerConfig.schema.json",
  "title": "Task Installer Configuration",
  "description": "getConfiguration() returns the raw TaskInstaller.toml text, not a structured object. Schema intentionally only checks that until this config is migrated to a named POJO.",
  "type": "string"
}
