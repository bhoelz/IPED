{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/MakePreviewConfig.schema.json",
  "title": "Make Preview Configuration",
  "description": "getConfiguration() returns a List<Set<String>> of MIME-type groups, not a structured object. Schema intentionally only checks that until this config is migrated to a named POJO.",
  "type": "array",
  "items": {
    "type": "array",
    "items": {
      "type": "string"
    }
  }
}
