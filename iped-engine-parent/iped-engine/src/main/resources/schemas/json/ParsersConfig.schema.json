{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://iped.digital-forensics.org/schema/ParsersConfig.schema.json",
  "title": "Parsers Configuration",
  "description": "Tika parser wiring (ParserConfig.xml). Currently stored and exposed as a raw XML document, not structured JSON — this schema intentionally only checks that, until ParserConfig.xml is migrated to a structured format.",
  "type": "string"
}
